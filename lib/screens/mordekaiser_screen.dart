import 'package:flutter/material.dart';

class MordekaiserScreen extends StatelessWidget {
   
  const MordekaiserScreen({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
         child: Text('Mordekaiser'),
      ),
    );
  }
}